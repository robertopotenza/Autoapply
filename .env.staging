# Staging Environment Configuration
# This file is loaded when NODE_ENV=staging

# Node Environment
NODE_ENV=staging

# PostgreSQL Database - Railway Staging
# For staging, you can use either:
# 1. Internal hostname (same as production): postgres.railway.internal
# 2. Public hostname for external testing: containers-us-west-xyz.railway.app
#
# Using public hostname for manual testing:
# DATABASE_URL=postgresql://postgres:PASSWORD@containers-us-west-xyz.railway.app:5432/railway
#
# Or use internal for deployed staging environment:
# DATABASE_URL=postgresql://postgres:PASSWORD@postgres.railway.internal:5432/railway

# OpenAI API Key
# OPENAI_API_KEY=sk-...

# JWT Secret (different from production!)
# JWT_SECRET=staging-jwt-secret

# Server Configuration
PORT=3000

# Logging Configuration (more verbose than production)
LOG_LEVEL=debug

# Database Pool Configuration
DB_POOL_MAX=15
DB_POOL_MIN=3
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=10000

# Scanning interval in hours
SCAN_INTERVAL_HOURS=2

# Automation mode
AUTOMATION_MODE=review

# CORS Origin
# CORS_ORIGIN=https://staging-app.railway.app

# Sentry (Optional)
# SENTRY_DSN=
# SENTRY_TRACES_SAMPLE_RATE=0.5
