# Development Environment Configuration
# This file is loaded when NODE_ENV=development (default)

# Node Environment
NODE_ENV=development

# PostgreSQL Database - Local Development
# Use localhost for local PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/autoapply

# Alternative: Individual connection parameters
# PGHOST=localhost
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=autoapply
# PGPORT=5432

# OpenAI API Key for AI-powered content generation
OPENAI_API_KEY=your_openai_api_key_here

# JWT Secret (use a different key than production!)
JWT_SECRET=dev-jwt-secret-change-this

# Server Configuration
PORT=3000

# Logging Configuration
LOG_LEVEL=debug

# Database Pool Configuration (optional - defaults are sensible)
DB_POOL_MAX=10
DB_POOL_MIN=2
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=10000
DB_STATEMENT_TIMEOUT=30000
DB_QUERY_TIMEOUT=30000

# Scanning interval in hours
SCAN_INTERVAL_HOURS=2

# Automation mode: auto | review
AUTOMATION_MODE=review

# CORS Origin (for local development)
CORS_ORIGIN=http://localhost:3000

# Sentry (Optional - usually disabled in dev)
# SENTRY_DSN=
# SENTRY_TRACES_SAMPLE_RATE=0.1
